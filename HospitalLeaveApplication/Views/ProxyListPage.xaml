<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HospitalLeaveApplication.Views.ProxyListPage"
             xmlns:viewmodel="clr-namespace:HospitalLeaveApplication.ViewModels"
             Title="Proxy List">

    <ContentPage.BindingContext>
        <viewmodel:ProxyListViewModel/>
    </ContentPage.BindingContext>
    
    <VerticalStackLayout>
        <StackLayout>
            <Frame Padding="10" Margin="10, 10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Label TextColor="Black" Text="Name" />
                    </Grid>
                    <Grid Grid.Column="1">
                        <Label TextColor="Black" Text="From" />
                    </Grid>
                    <Grid Grid.Column="2">
                        <Label TextColor="Black" Text="To" />
                    </Grid>
                    <Grid Grid.Column="3">
                        <Label TextColor="Black" Text="Leave type" />
                    </Grid>
                </Grid>
            </Frame>
            <CollectionView ItemsSource="{Binding LeaveApplicationList}"
                            SelectionMode="Single"
                            EmptyView="No items found"
                            SelectionChanged="CollectionView_SelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="20, 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0">
                                <Label Text="{Binding Name}" />
                            </Grid>
                            <Grid Grid.Column="1">
                                <Label Text="{Binding FromDate, StringFormat='{0:dd-MM-yy}'}" />
                            </Grid>
                            <Grid Grid.Column="2">
                                <Label Text="{Binding ToDate, StringFormat='{0:dd-MM-yy}'}" />
                            </Grid>
                            <Grid Grid.Column="3">
                                <Label Text="{Binding LeaveType}" />
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </VerticalStackLayout>
</ContentPage>